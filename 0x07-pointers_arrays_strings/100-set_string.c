#include "main.h"

/**
 * set_string - this sets a pointer val to the char efficiently only.
 *
 * @s: this is a string doublt pointer only.
 * @to: this is a string pointer only.
 */

void set_string(char **s, char *to)
{
	*s = to;
}
